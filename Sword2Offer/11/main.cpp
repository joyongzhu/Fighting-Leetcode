#include <vector>

class Solution {
public:
    int minArray(std::vector<int>& numbers) {
        int left = 0, right = numbers.size();

        while (left < right) {
            int mid = (left + right)/2;
            if (mid)
        }
    }
};